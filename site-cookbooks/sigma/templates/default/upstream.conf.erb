#!upstart

start on (local-filesystems and net-device-up IFACE!=lo)
stop on [!12345]

console log

env NODE_ENV=production
env HOME=/var/www
setuid www-data
setgid www-data
chdir /var/www/sigma/build
exec node <%= @service[:script] %>
